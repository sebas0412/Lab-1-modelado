from statistics import variance
import numpy as np

def simplex(objective,restrictions,variables,maximize):

    return variables

objective = np.array([30.0, 100.0, 0, 0, 0, -100000.0])
restrictions = np.matrix('1 2; 3 4')
print(objective[0])
restrictions

print(type(objective))